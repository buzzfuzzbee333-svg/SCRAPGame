{
  "name": "last-scrap",
  "version": "0.1.0",
  "private": true,
  "description": "Engine-agnostic TypeScript core scaffold for the Last Scrap vertical slice.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
